# Загрузка PhotoMaker

Перед вставкой выберите существующий файл вместо `__SELECT_PHOTOMAKER_MODEL__`. В чистой установке список пуст, пока модель не помещена в каталог `models/photomaker`. Фрагмент проверен по схеме, но веса не загружались.
