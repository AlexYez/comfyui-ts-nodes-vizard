# Сохранение JSON в output

Подайте JSON-строку на вход. Нода создаст файл в подпапке `Text` с prefix `report` и счётчиком. Некорректный JSON всё равно получит расширение `.json`, поэтому проверяйте содержимое заранее.
