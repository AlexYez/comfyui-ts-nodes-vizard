# Сохранить обученную LoRA

Подключите именно `LORA_MODEL` из обучения. Фрагмент не задаёт `steps`, поэтому нода использует имя со счётчиком без отметки числа шагов. Реальная запись весов не выполнялась.
