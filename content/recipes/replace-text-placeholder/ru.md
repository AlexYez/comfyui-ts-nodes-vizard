# Подстановка буквального плейсхолдера

Внешний `STRING` заменяет каждое точное `{prompt}`. Официальные шаблоны используют тот же приём для `{prompt}`, `{width}`, `{height}`, `USER_INPUT` и других маркеров. Полный внешний workflow не переносится.
