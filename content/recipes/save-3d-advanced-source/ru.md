# File3D через Save3DAdvanced

Подайте готовый `FILE_3D` и состояние живого Load3D-виджета в `Save3DAdvanced`. Fragment оставляет `filename_prefix = 3d/ComfyUI`, `width = 1024` и `height = 1024`; optional camera/model-info не подключены и берутся из `viewport_state`.

Это source-derived fragment. В workflow wheel 0.1.42 exact-нода не встречается, поэтому здесь нет producer файла, вымышленного состояния камеры или полного workflow.
