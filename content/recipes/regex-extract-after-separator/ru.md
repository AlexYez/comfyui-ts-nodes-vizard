# Текст после разделителя

Фрагмент повторяет шаблон из официальных audio/image speech workflows и возвращает группу 1 после строки `---`. Если разделителя нет или regex некорректен, выход будет пустым.
