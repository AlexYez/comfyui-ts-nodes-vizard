# Соединение частей подсказки

Фрагмент соединяет `black and white photograph` и `realistic detail` через `, `. Официальные шаблоны используют такой delimiter для сборки подсказок. Пустая часть не удаляется автоматически и оставит разделитель.
