# Локальная генерация текста

Подключите CLIP с поддержкой `generate`, а не обычный diffusion-only text encoder. Media-порты оставлены внешней задачей. Параметры повторяют распространённый официальный пресет; модель не запускалась.
