# Проверить масштаб query-проекции CLIP

Фрагмент меняет только `q`: `0.90`; `k`, `v` и `out` остаются равными `1`. Подайте output в `CLIPTextEncode` и сравните его с прямым подключением того же CLIP на одинаковом prompt.

Перед оценкой убедитесь, что state dict содержит поддерживаемые окончания `self_attn.q_proj.weight` и `.bias`. Если ключи названы иначе, нода вернёт clone без изменённых параметров.

Официальных workflow-case 0.1.42 нет. Fragment и exact suffix matching проверены без весов; transformer forward и влияние на изображения не исполнялись.
