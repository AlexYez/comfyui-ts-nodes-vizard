# Оставить первые 77 кодовых точек

Подключите один `STRING`. Нода вернёт `texts[:77]`: короткая строка не изменится, длинная оборвётся строго по Python-кодовым точкам. Это не предел токенов и не поиск границы слова.

Проверьте окончание, если используются emoji или комбинируемые знаки. Значение 77 взято из default схемы, а не из официального workflow; fragment не импортировался и не выполнялся.
