# Развернуть внешнее расписание сигм

Фрагмент вставляет одну `FlipSigmas` с внешним входом. Сначала сравните значения: список `[4, 2, 0]` станет `[0.0001, 2, 4]`, а не чистым разворотом с нулём в начале.

В официальных 512 JSON-файлах 0.1.42 эта нода не встречается. Поэтому рецепт служит для проверки точного поведения и не предлагает готовую sampler-топологию.

Fragment прошёл проверку схемы и tensor probe. Следующий сэмплер не запускался; workflow-поле отсутствует.
