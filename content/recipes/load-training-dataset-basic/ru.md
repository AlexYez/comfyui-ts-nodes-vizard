# Выбрать ранее сохранённый набор

Замените `SELECT_SAVED_DATASET` реальным значением из `folder_name`. Это динамический `COMBO`, поэтому каталог нельзя честно закрепить в переносимом fragment. Проверьте источник файлов и сравните длины двух выходных списков после загрузки.

Placeholder должен блокировать исполнение до ручного выбора. Официального workflow с нодой нет; fragment не импортировался и не выполнялся. Временный probe читал только созданные им синтетические shard-файлы.
