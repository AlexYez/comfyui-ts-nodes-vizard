# Отдельный текст для ControlNet

Фрагмент сохраняет основной conditioning и добавляет ключи `cross_attn_controlnet` и `pooled_output_controlnet`. Эффект возможен только с реализацией ControlNet, которая читает эти ключи. Модельный граф не исполнялся.
