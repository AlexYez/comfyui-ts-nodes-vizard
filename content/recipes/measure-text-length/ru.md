# Измерение строки перед срезом

Подайте `STRING` на внешний вход. `StringLength` вернёт число кодовых точек Python. Для ограничения контекста модели требуется токенизатор; этот фрагмент его не заменяет.
