# Поиск строки ERROR

`multiline=true` заставляет `^` проверять начало каждой строки. Для заданного текста результат `true`. Неверный regex вернул бы `false`, поэтому шаблон стоит проверять отдельно.
