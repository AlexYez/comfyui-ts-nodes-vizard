# Извлечь LoRA из разности MODEL

Подайте разность из `ModelMergeSubtract`, а не обычную базовую модель. Rank 8 ограничивает low-rank часть, `bias_diff=true` сохраняет изменения bias. Весовой граф не исполнялся.
