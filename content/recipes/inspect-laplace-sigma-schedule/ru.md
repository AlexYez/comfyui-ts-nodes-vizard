# Проверить пять сигм Лапласа

Fragment создаёт `LaplaceScheduler` с пятью точками и значениями по умолчанию. Exact-source probe получил приблизительно `[14.614642, 1.414199, 1.0, 0.707114, 0.0291675]`.

Нода возвращает ровно пять элементов и не добавляет ноль. При прямом подключении custom sampler увидит четыре перехода и закончит на `sigma_min`. Проверьте, соответствует ли ненулевой финал методике модели, прежде чем запускать denoise.

В официальных workflow 0.1.42 `LaplaceScheduler` не найден. Fragment нужен для воспроизводимой проверки функции, не содержит модели или sampler и не имеет поля полного workflow.
