{
  "KSampler": {
    "input": {
      "required": {
        "model": ["MODEL", { "tooltip": "The model used for denoising the input latent." }],
        "seed": ["INT", { "default": 0, "min": 0, "max": 18446744073709551615, "control_after_generate": true }],
        "steps": ["INT", { "default": 20, "min": 1, "max": 10000 }],
        "cfg": ["FLOAT", { "default": 8.0, "min": 0.0, "max": 100.0, "step": 0.1, "round": 0.01 }],
        "sampler_name": [["euler", "euler_ancestral", "dpmpp_2m"], { "tooltip": "The algorithm used when sampling." }],
        "scheduler": [["normal", "karras", "exponential"], { "tooltip": "The scheduler controls how noise is gradually removed." }],
        "positive": ["CONDITIONING", {}],
        "negative": ["CONDITIONING", {}],
        "latent_image": ["LATENT", {}],
        "denoise": ["FLOAT", { "default": 1.0, "min": 0.0, "max": 1.0, "step": 0.01 }]
      }
    },
    "input_order": {
      "required": ["model", "seed", "steps", "cfg", "sampler_name", "scheduler", "positive", "negative", "latent_image", "denoise"]
    },
    "output": ["LATENT"],
    "output_is_list": [false],
    "output_name": ["LATENT"],
    "output_tooltips": ["The denoised latent."],
    "output_node": false,
    "name": "KSampler",
    "display_name": "KSampler",
    "description": "Uses the provided model, positive and negative conditioning to denoise the latent image.",
    "python_module": "nodes",
    "category": "model/sampling",
    "deprecated": false,
    "experimental": false,
    "dev_only": false,
    "api_node": false,
    "search_aliases": ["sampler", "sample", "generate", "denoise", "diffuse", "txt2img", "img2img"]
  },
  "CheckpointLoaderSimple": {
    "input": {
      "required": {
        "ckpt_name": [["example-a.safetensors", "example-b.safetensors"], { "tooltip": "The name of the checkpoint (model) to load." }]
      }
    },
    "input_order": {
      "required": ["ckpt_name"]
    },
    "output": ["MODEL", "CLIP", "VAE"],
    "output_is_list": [false, false, false],
    "output_name": ["MODEL", "CLIP", "VAE"],
    "output_tooltips": [
      "The model used for denoising latents.",
      "The CLIP model used for encoding text prompts.",
      "The VAE model used for encoding and decoding images to and from latent space."
    ],
    "output_node": false,
    "name": "CheckpointLoaderSimple",
    "display_name": "Load Checkpoint",
    "description": "Loads a diffusion model checkpoint, diffusion models are used to denoise latents.",
    "python_module": "nodes",
    "category": "model/loaders",
    "deprecated": false,
    "experimental": false,
    "dev_only": false,
    "api_node": false,
    "search_aliases": ["load model", "checkpoint", "model loader", "load checkpoint", "ckpt", "model"]
  },
  "SaveImage": {
    "input": {
      "required": {
        "images": ["IMAGE", { "tooltip": "The images to save." }],
        "filename_prefix": ["STRING", { "default": "ComfyUI" }]
      },
      "hidden": {
        "prompt": "PROMPT",
        "extra_pnginfo": "EXTRA_PNGINFO"
      }
    },
    "input_order": {
      "required": ["images", "filename_prefix"],
      "hidden": ["prompt", "extra_pnginfo"]
    },
    "output": ["IMAGE"],
    "output_is_list": [false],
    "output_name": ["images"],
    "output_node": true,
    "name": "SaveImage",
    "display_name": "Save Image",
    "description": "Saves the input images to your ComfyUI output directory.",
    "python_module": "nodes",
    "category": "image",
    "deprecated": false,
    "experimental": false,
    "dev_only": false,
    "api_node": false,
    "search_aliases": ["save", "save image", "export image", "output image", "write image", "download"]
  }
}
